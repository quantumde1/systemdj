{
    "name": "mdev Device Manager",
    "executable": "udevd --daemon && udevadm trigger --action=add    --type=subsystems && udevadm trigger --action=add    --type=devices && udevadm trigger --action=change --type=devices && udevadm settle",
    "respawn": 5,
    "deps": ["remount", "premount", "hwdrivers"]
}