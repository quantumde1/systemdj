{
    "name": "Mounting all other filesystems and creating files",
    "executable": "mount --all --test-opts && mount tmpfs /tmp -t tmpfs && mkdir /run/dbus",
    "respawn": 5,
    "deps": ["remount", "premount"],
    "type": "oneshot"
}